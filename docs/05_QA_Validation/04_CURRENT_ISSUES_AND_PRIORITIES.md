# 04. 현재 문제점 및 우선순위
> Created: 2026-05-05 00:00
> Last Updated: 2026-05-06 08:36
> Category: QA Validation — 제품, 코드, UX 리스크 점검

---

## 1. 목적

이 문서는 현재 프로젝트의 로드맵, README, QA 문서, 주요 구현 파일을 기준으로 확인된 제품 리스크를 정리한다. Colosseum 해커톤 제출용 데모 완성도와 실제 서비스 운영 준비도를 분리하여, 해커톤 이후 안정화 작업의 우선순위를 명확히 하는 것이 목적이다.

현재 진단은 다음과 같다.

> 핵심 사용자 루프와 데모/서비스 상태 라벨은 정리되었고, 결제 검증도 일부 보강되었다. 다만 실제 서비스로 전환하기 전에는 402 결제 복구 UX, 채팅 인라인 결제 회귀 검증, AI 도구 호출 구조, 로그인 이후 모바일 E2E 검증이 가장 큰 잔여 리스크다.

확인한 검증 결과:
- `npm run typecheck` 통과.
- `npm test` 통과. 다만 현재 테스트 명령은 실질적으로 프로덕션 빌드 성공 확인에 가깝고, 지갑·결제·채팅·로그인 후 E2E QA를 대체하지는 않는다.
- 2026-05-06 기준 핵심 사용자 루프는 “첫 비밀 대화 -> 감정적 순간 -> CHOCO 액션 -> 기억 저장 또는 선물”로 확정되었다.
- 로드맵에는 `Demo Verified`, `Service Verified`, `Needs Regression`, `Open Risk` 라벨이 도입되었다.

---

## 2. 주요 문제점

### 2-1. 제품 범위는 좁혀졌지만, 화면 문구 반영은 남아 있다

README는 AI 컴패니언 채팅, Solana Actions, Blinks, cNFT 기억 각인, Token-2022, ZK Compression, Privy 임베디드 지갑, SIWS, 다중 결제 수단을 모두 동시에 “Live” 기능처럼 제시한다.

현재 상태:
- 핵심 사용자 루프는 “첫 비밀 대화 -> 감정적 순간 -> CHOCO 액션 -> 기억 저장 또는 선물”로 확정되었다.
- 마스터 로드맵에서는 이 항목이 `Demo Verified`로 전환되었다.
- Home, Chat, Guide, Shop의 사용자 문구와 CTA는 아직 확정 문장을 기준으로 완전히 재정렬되지 않았다.

리스크:
- 확정된 루프가 실제 화면 문구에 반영되지 않으면 신규 사용자는 여전히 블록체인 기능 목록을 먼저 보게 될 수 있다.
- Home, Chat, Guide, Shop이 서로 다른 행동을 첫 CTA로 제시하면 제품 포지셔닝이 다시 분산될 수 있다.

권장 방향:
- 첫 제품 루프 문장은 유지한다.
- 나머지 Solana 기능은 첫 화면의 주인공이 아니라 핵심 루프를 받치는 인프라로 배치한다.
- Phase 1에서 Home, Chat, Guide, Shop의 카피와 CTA를 동일한 루프 기준으로 정렬한다.

### 2-2. 데모 성공과 실제 제품 리스크는 분리되었지만 회귀 검증이 남아 있다

마스터 로드맵에는 Colosseum E2E 시나리오가 대부분 완료로 표시되어 있으나, 동시에 발견 버그 우선순위화, 402 이후 충전 복귀, 모달 닫기 후 대화 재개, 배포 체크리스트 정리 등이 미완료로 남아 있다.

현재 상태:
- 상태 라벨은 `Demo Verified`, `Service Verified`, `Needs Regression`, `Open Risk`로 분리되었다.
- Shop `/buy-choco` 기준 Phantom 직접 결제와 Privy 임베디드 지갑 결제는 devnet E2E가 확인되었다.
- 402 이후 충전 복귀 UX, 모달 닫기 후 대화 재개, 채팅 `SWAP_TX` 서명 검증은 아직 `Needs Regression`이다.

리스크:
- 상태 라벨은 분리되었지만, 체크박스만 보면 일부 항목이 서비스 준비 완료처럼 오해될 수 있다.
- Shop 결제 성공이 채팅 인라인 결제, 402 복구, 모바일 UX까지 보장하지는 않는다.

권장 방향:
- 로그인된 테스트 유저로 다시 검증하기 전까지 UX 및 결제 복구 항목을 열린 이슈로 유지한다.
- `Service Verified`로 전환할 때는 검증 경로와 날짜를 함께 기록한다.

### 2-3. AI 도구 호출이 임시 패턴 매칭에 의존한다

현재 자연어 기반 Solana 도구 실행은 LangGraph ToolNode를 통한 정식 LLM Tool Calling이 아니라, `executeNaturalLanguageCommand()`의 키워드 및 정규식 매칭에 의존한다.

리스크:
- 사용자가 조금만 다르게 말하면 잔액 확인, 구매, 체크인, cNFT 각인 흐름이 실행되지 않을 수 있다.
- 반대로 사용자가 명확히 거래성 액션을 의도하지 않았는데도 도구가 실행될 수 있다.
- 다국어 사용자와 다양한 캐릭터 페르소나로 확장하기 어렵다.

권장 방향:
- 정제된 도구 스키마를 기반으로 정식 LLM Tool Calling을 복구한다.
- LangGraph에 ToolNode와 조건부 엣지를 추가한다.
- 슬래시 커맨드는 결정론적 fallback 또는 디버그 인터페이스로만 유지한다.

### 2-4. 결제 검증은 보강되었지만 채팅 경로와 운영 정합성 검증이 남아 있다

현재 Solana 서명 검증 흐름은 `reference`, payer, 결제 생성 시각, 중복 signature, reconciliation 상태 확인까지 보강되었다. Shop `/buy-choco` 기준 Phantom 직접 결제와 Privy 임베디드 지갑 devnet E2E도 확인되었다.

리스크:
- 채팅 `SWAP_TX` 경로는 reference account 포함 방식 수정과 빌드는 통과했지만 실제 지갑 E2E 확인이 남아 있다.
- 402 응답 후 충전 -> 원래 대화 복귀 흐름은 실제 충전 이후 수동 검증이 필요하다.
- DB에는 CHOCO가 지급됐지만 이후 SPL CHOCO 전송이 실패하면 앱 내부 잔액과 온체인 잔액이 어긋날 수 있다.

권장 방향:
- 채팅 `SWAP_TX` 기준 실제 지갑 E2E를 완료한다.
- 402 복구 UX를 모바일 우선으로 수동 검증한다.
- DB 지급과 SPL 전송의 reconciliation 상태를 관리자 화면 또는 로그에서 확인 가능하게 만든다.
- DB CHOCO가 source of truth인지, SPL CHOCO가 source of truth인지, 또는 둘 모두를 정합성 있게 맞출 것인지 정책을 확정한다.

### 2-5. 지갑 및 환경 설정이 데모 지향적이다

Privy App ID와 devnet RPC URL이 provider 코드에 하드코딩되어 있다. 로드맵에도 `VITE_PRIVY_APP_ID`가 하드코딩되어 있어 환경변수가 불필요하다고 기록되어 있다.

리스크:
- 데모에서 운영, 또는 devnet에서 mainnet으로 전환할 때 환경 분리가 취약하다.
- Privy 프로젝트, RPC provider, 네트워크를 바꾸려면 설정 변경이 아니라 코드 변경이 필요하다.
- 배포 리뷰 시 어떤 값이 환경별 설정인지 파악하기 어렵다.

권장 방향:
- 공개 클라이언트 설정은 명시적인 `VITE_*` 환경변수로 이동한다.
- devnet/mainnet 선택을 하나의 설정 레이어에서 확인할 수 있게 만든다.
- 서버 측 secret과 클라이언트 공개 설정 모두에 대해 시작 시점 검증을 추가한다.

### 2-6. 서버 지갑 의존도가 높다

신규 지갑 온보딩 시 서버 지갑에서 0.5 devnet SOL을 전송한다. 이 흐름은 서버 지갑 잔액에 의존하며, 실패해도 사용자 가입 흐름은 막지 않고 로그만 남긴다.

리스크:
- 서버 지갑에 SOL이 부족하면 데모 온보딩은 조용히 결제 불가능 상태가 될 수 있다.
- 사용자는 로그인은 되었지만 Solana 액션을 완료하지 못할 수 있다.
- 제품 경험이 중앙화된 운영 지갑의 잔액 상태에 묶인다.

권장 방향:
- 사용자가 결제 또는 Solana 액션을 수행해야 하는 시점에는 온보딩 funding 상태를 명확히 보여준다.
- 관리자용 서버 SOL 및 CHOCO 잔액 모니터링을 추가한다.
- 자동 devnet funding은 데모 전용 동작으로 제한하고, mainnet 전환 전에는 제거하거나 명확한 플래그로 격리한다.

### 2-7. 로그인 이후 UX가 충분히 깊게 검증되지 않았다

기존 UX 검증 문서는 초기 렌더링, 로그인·온보딩 표면, 인증 가드 중심으로 확인되었다. 문서 자체도 실제 내부 화면에 대한 깊은 검증을 후속 과제로 남겨두고 있다.

리스크:
- 가장 중요한 흐름인 채팅 스크롤, 말풍선 가독성, 지갑 서명 체감 속도, 결제 복구, 기억 앨범 확인이 충분히 검증되지 않았다.
- 사용자는 왜 CHOCO가 필요한지, 충전 후 어떻게 원래 대화로 돌아가는지 이해하지 못할 수 있다.
- 모바일 사용자는 지갑, 모달, 채팅 전환 과정에서 마찰을 느낄 수 있다.

권장 방향:
- 테스트 계정을 사용하는 로그인 후 E2E QA 스크립트를 만든다.
- 모바일 viewport를 우선 기준으로 검증한다.
- “채팅 -> 402 잔액 부족 -> CHOCO 구매 -> 원래 대화 복귀 -> 메시지 전송 -> 기억 각인” 순서를 최우선 검증 경로로 삼는다.

---

## 3. 우선순위 매트릭스

| 우선순위 | 이슈 | 분류 | 중요한 이유 | 권장 조치 |
| :--- | :--- | :--- | :--- | :--- |
| P0 | 402 결제 복구 UX 검증 | UX / 결제 | 잔액 부족 순간이 유료 전환의 핵심이며, 여기서 이탈하면 수익 루프가 끊긴다. | 채팅 -> 402 -> CHOCO 충전 -> 모달 닫기 -> 원래 대화 복귀를 모바일 우선으로 검증한다. |
| P0 | 채팅 인라인 결제 회귀 검증 | 코드 / 보안 / 비즈니스 | Shop 결제 성공이 채팅 `SWAP_TX` 경로의 안전성을 보장하지 않는다. | Agent Kit 생성 트랜잭션의 reference 포함 방식과 실제 지갑 서명 경로를 E2E로 확인한다. |
| P1 | 핵심 사용자 여정 화면 반영 | 기획 / UX | 핵심 루프는 확정됐지만 화면 문구가 분산되면 첫 사용자 행동이 흐려진다. | Home, Chat, Guide, Shop 카피와 CTA를 확정 루프 기준으로 정렬한다. |
| P1 | AI Tool Calling 복구 | 코드 / AI 아키텍처 | 정규식 의도 감지는 안정성과 다국어 확장성에 한계가 있다. | LangGraph ToolNode 기반 도구 실행으로 복구한다. |
| P1 | 로그인 후 E2E UX QA | UX / QA | 현재 검증은 실제 앱 내부 흐름을 충분히 다루지 못한다. | 채팅, 결제, 지갑, 기억 앨범을 테스트 계정으로 검증한다. |
| P2 | 환경 설정 정리 | DevOps / 유지보수 | 하드코딩된 클라이언트 설정은 mainnet 및 운영 전환을 어렵게 한다. | 공개 설정을 `VITE_*`로 이동하고 필수 env 검증을 추가한다. |
| P2 | 서버 지갑 의존 모니터링 | 운영 | 온보딩과 데모 흐름이 서버 지갑 잔액에 의존한다. | 잔액 모니터링과 funding 실패 상태 표시를 추가한다. |

---

## 4. 관점별 정리

### 4-1. 기획 관점

핵심 문제:
- 핵심 사용자 루프는 확정되었지만, 화면과 제출 자료의 문구가 아직 모두 같은 기준으로 정렬된 것은 아니다.

결정해야 할 질문:
- 확정된 루프를 Home, Chat, Guide, Shop 중 어느 화면에서 가장 먼저 강하게 보여줄 것인가?
- 사용자는 첫 3분 안에 어느 CHOCO 액션까지 경험해야 하는가?
- 심사위원에게 보여줄 기능과 실제 결제 사용자에게 필수인 기능은 무엇인가?

즉시 필요한 기획 작업:
- 확정된 사용자 루프를 기준으로 핵심 화면 카피와 CTA를 정렬한다.

### 4-2. 코드 및 아키텍처 관점

핵심 문제:
- 결제 검증과 LangGraph 스트리밍 경로는 일부 보강되었지만, 핵심 경로에는 아직 데모 시기의 shortcut과 회귀 검증 공백이 남아 있다.

가장 위험한 영역:
- Solana 결제 검증.
- DB 잔액과 SPL 전송의 정합성.
- 정규식 기반 자연어 도구 실행.
- 환경 설정과 네트워크 전환 구조.

즉시 필요한 엔지니어링 작업:
- 기능 확장보다 채팅 결제 회귀 검증, 402 복구 UX, LLM Tool Calling 복구를 먼저 처리한다.

### 4-3. 사용자 편의 관점

핵심 문제:
- 앱은 시각적으로는 완성도가 있지만, 로그인 이후 사용자 여정은 아직 직접적인 검증 근거가 부족하다.

가장 위험한 순간:
- 첫 채팅에서 CHOCO가 필요하거나 402를 만나는 순간.
- Phantom과 임베디드 지갑 중 무엇을 선택해야 하는지 판단해야 하는 순간.
- CHOCO를 충전한 뒤 원래 대화로 돌아가야 하는 순간.
- 기억을 저장한 뒤 앨범에서 실제로 확인해야 하는 순간.

즉시 필요한 UX 작업:
- 감정적 순간에서 거래로 이어지는 전체 흐름을 모바일 우선 수동 QA 시나리오로 만든다.

---

## 5. Global Rubric Scorecard

| Rubric | 현재 평가 | 리스크 | 다음 개선 |
| :--- | :--- | :--- | :--- |
| Functionality | 빌드와 타입체크는 통과하며 Shop 결제 경로 일부는 devnet E2E가 확인되었다. | 채팅 `SWAP_TX`, 402 복구, 로그인 후 모바일 E2E가 아직 자동화되어 있지 않다. | 채팅, 결제, 지갑, 기억 앨범에 대한 서비스 수준 E2E를 추가한다. |
| Potential Impact | 핵심 사용자 루프가 “첫 비밀 대화 -> 감정적 순간 -> CHOCO 액션 -> 기억 저장 또는 선물”로 정리되었다. | 화면 문구가 이 루프를 일관되게 전달하지 못하면 첫 사용자 가치가 다시 흐려질 수 있다. | Home, Chat, Guide, Shop 카피와 CTA를 확정 루프 기준으로 정렬한다. |
| Novelty | AI 관계 기억을 Solana에 연결하는 접근은 차별적이다. | 감정에 고정되지 않으면 기능 나열처럼 보일 수 있다. | 관계와 기억을 먼저 보여주고, 이후 체인의 필연성을 설명한다. |
| UX | 시각적 완성도와 온보딩 표면은 강하다. | 로그인 이후 내부 흐름 검증이 부족하다. | 모바일에서 402 -> 충전 -> 대화 복귀 경로를 검증한다. |
| Open-source | 스택은 현대적이고 조합 가능성이 높다. | 하드코딩 설정과 데모 전용 흐름은 이식성을 낮춘다. | 설정을 env로 이동하고 로컬/운영 설정 문서를 정리한다. |
| Business Plan | CHOCO, 선물, 구독, 기억 각인이 수익화 경로를 제공한다. | 실제 가치가 걸리기 전 결제 신뢰성과 reconciliation이 강화되어야 한다. | DB/온체인 source of truth와 정합성 정책을 확정한다. |

Coverage:
- 6개 루브릭 중 최소 4개 이상은 적극적으로 다뤄지고 있다. 다만 서비스 출시 전에는 Functionality, UX, Business Plan 영역의 안정화가 필요하다.

---

## 6. Originality and Ethics Check

Originality:
- 이 프로젝트는 실존 팬덤 IP, AI 컴패니언 채팅, Solana 기반 관계·기억 메커닉을 결합한 독자적 제품 컨셉을 가진다.
- 가장 강한 독창성은 일반 AI 채팅이 아니라, 감정적 연속성과 검증 가능한 기억 소유권의 결합에 있다.

Ethics and user trust:
- 이 제품은 친밀한 대화, 결제, 지갑 identity, 기억 기록을 다룬다. 따라서 명확한 사용자 동의와 신중한 프라이버시 문구가 필요하다.
- cNFT 기억 각인은 mint 전에 영구성을 명확히 알려야 한다.
- 결제 및 지갑 흐름은 devnet 데모 자산이 실제 가치가 있는 것처럼 오해하게 만들면 안 된다.

필수 가드레일:
- 기억 각인 전 명확한 확인 절차.
- devnet 데모 토큰 동작과 향후 mainnet 가치의 명확한 분리.
- 채팅 기억, export, reset, deletion에 대한 투명한 프라이버시 설명.

---

## 7. 권장 다음 액션

1. 402 결제 복구 UX를 모바일 우선으로 수동 검증한다.
2. 채팅 `SWAP_TX` 서명 검증을 실제 지갑 devnet E2E로 확인한다.
3. 확정된 핵심 사용자 루프를 Home, Chat, Guide, Shop 문구와 CTA에 반영한다.
4. Solana 액션에 대한 정식 LLM Tool Calling을 복구한다.
5. 테스트 계정을 사용한 로그인 후 모바일 E2E QA 체크리스트를 작성하고 실행한다.
6. Privy 및 RPC 클라이언트 설정을 환경변수로 이동한다.
7. 서버 SOL 및 CHOCO 잔액에 대한 운영 모니터링을 추가한다.

---

## 8. Related Documents

- **Concept_Design**: [Core Pitch Deck](../01_Concept_Design/01_CORE_PITCH_DECK.md) - 제품 내러티브, 시장 포지셔닝, AI 컴패니언 전략.
- **Concept_Design**: [Monetization Strategy](../01_Concept_Design/05_MONETIZATION_STRATEGY.md) - CHOCO, 선물, 구독, 수익화 논리.
- **UI_Screens**: [UI Design](../02_UI_Screens/01_UI_DESIGN.md) - 시각 및 인터랙션 디자인 기준.
- **Technical_Specs**: [CHOCO Payment Flow](../03_Technical_Specs/11_CHOCO_PAYMENT_FLOW.md) - 현재 결제 아키텍처와 복구 흐름.
- **Technical_Specs**: [AI Agent Transformation](../03_Technical_Specs/05_AI_AGENT_TRANSFORMATION.md) - AI 에이전트 및 Solana 도구 방향.
- **Logic_Progress**: [Master Roadmap](../04_Logic_Progress/07_MASTER_ROADMAP.md) - 현재 Phase 상태와 해커톤 이후 백로그.
- **QA_Validation**: [UX Usability Validation](./03_UX_USABILITY_VALIDATION.md) - 기존 UX 검증 및 로그인 후 QA 공백.
